type ExpSelectValue = {
  value: number;
  label: string;
};

export type ExpSelectOption = {
  label: string;
  key: string;
  values: ExpSelectValue[];
};

export type ExpInputOption = {
  label: string;
  key: string;
};
